# ── R / sistema ───────────────────────────────────────────────────
.Rproj.user
.Rhistory
.RData
.Ruserdata
.DS_Store
__pycache__/
*.pyc
.venv/
venv/
.env
.env.*

# ── Scrapers (local — no van a GitHub) ────────────────────────────
scrapers/

# ── Datos (raw + processed + scripts de build) ────────────────────
data/
neogremialismo-data/
*.db
*.sqlite
*.sqlite3
config/settings.py

# ── Outputs: CSV/dumps no; imágenes de análisis sí ────────────────
outputs/volatilidad/**
!outputs/volatilidad/.gitkeep

# Dependencias de htmlwidgets (plotly/crosstalk) — no versionar
outputs/imagenes/**/*_files/

# Figuras PNG/SVG/PDF de análisis SÍ se versionan
# (todo lo demás bajo outputs/imagenes/ queda trackeable)

# ── Generados / ruido ─────────────────────────────────────────────
README.html
*.html
!docs/**/*.html
